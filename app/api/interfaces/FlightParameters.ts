export interface FlightParameters {
    velocity?: number,
    elevation?: number,
    searchRadius?: number,
    searchRadiusOverlap?: number,
    acceptanceRadius?: number,
    pathRotation?: number,
}
